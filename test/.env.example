# Test Environment Configuration for GearMeshing-AI
# 
# This file contains environment variables for testing purposes only.
# Copy this file to .env and fill in your actual API keys.
#
# IMPORTANT: Never commit real API keys to version control!

# AI Provider API Keys
# Get these from the respective provider websites:
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/
# - Google: https://makersuite.google.com/app/apikey

OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Test Execution Configuration
# Control which tests run and their behavior

# Run tests that call real AI models (set to false for faster unit tests)
RUN_AI_TESTS=true

# Run slow integration tests (set to false for faster test suites)
RUN_SLOW_TESTS=false

# Timeout for individual tests in seconds
TEST_TIMEOUT=60

# Optional: Custom model configurations for testing
# These override the default test configurations

# OpenAI test model
# TEST_OPENAI_MODEL=gpt-4
# TEST_OPENAI_TEMPERATURE=0.7
# TEST_OPENAI_MAX_TOKENS=1000

# Anthropic test model
# TEST_ANTHROPIC_MODEL=claude-3-sonnet-20240229
# TEST_ANTHROPIC_TEMPERATURE=0.7
# TEST_ANTHROPIC_MAX_TOKENS=1000

# Gemini test model
# TEST_GEMINI_MODEL=gemini-pro
# TEST_GEMINI_TEMPERATURE=0.7
# TEST_GEMINI_MAX_TOKENS=1000

# Optional: Database configuration for integration tests
# These are only needed if running database-related smoke tests

# TEST_DATABASE_URL=postgresql://user:password@localhost:5432/gearmeshing_test
# TEST_REDIS_URL=redis://localhost:6379/0

# Optional: MCP Gateway configuration
# Only needed for MCP-related tests

# TEST_MCP_GATEWAY_URL=http://localhost:4445
# TEST_MCP_GATEWAY_TOKEN=your_mcp_gateway_token

# Optional: Logging configuration for tests
# Control test output verbosity

# TEST_LOG_LEVEL=INFO
# TEST_LOG_FORMAT=simple

# Optional: Monitoring configuration
# Enable LangSmith tracing for test runs

# LANGSMITH_TRACING=false
# LANGSMITH_API_KEY=your_langsmith_api_key
# LANGSMITH_PROJECT=gearmeshing-ai-test
